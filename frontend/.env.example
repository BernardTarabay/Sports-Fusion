# The frontend talks to the real API through the Vite proxy (see vite.config.js), so this
# file is optional -- copy it to .env only if you want the offline mock fixture instead.
#
# VITE_USE_MOCK=true   # 140 invented players and 39 invented games, no backend needed
